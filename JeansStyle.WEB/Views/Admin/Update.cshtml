@using JeansStyle.BLL.DTOs
@model             Product
@{
    Layout = "_Admin";
    ViewData["Title"] = "Product updating";
    var categories = ViewBag.Categories as List<CategoryDto>;
}

@section Breadcrumbs
{
<li class="breadcrumb-item">Products</li>
}

<div class="main-content">
    <form method="post" enctype="multipart/form-data">
        <input type="text" value=@Model.Id name="id" /><br /><br />


        <label for="title">Title:</label><br />
        <input type="text" value=@Model.Title name="title" /><br /><br />

        <label for="price">Price:</label><br />
        <input type="text" value=@Model.Price name="price" /><br /><br />

        <label>Description:</label><br />
        <input type="description" value=@Model.Description name="description" /><br /><br />


        <select multiple name="category">
            @foreach (var category in categories)
            {
                <option value=@category>@category.Name</option>
            }

        </select>

        <label for="season">Choose the season:</label><br />
        <select value=@Model.Seasons multiple name="season">
            <option value=0>Winter</option>
            <option value=1>Spring</option>
            <option value=2>Summer</option>
            <option value=3>Autumn</option>

        </select><br /><br />

        <label for="gender">Choose the gender:</label>
        <select value=@Model.Gender name="gender">
            <option value=0>Male</option>
            <option value=1>Female</option>
            <option value=2>Unisex</option>
        </select><br /><br />

        <div class="form-group">
            <label asp-for=@Model.Image class="control-label"></label>
            <div class="custom-file">
                <input asp-for=@Model.Image class="custom-file-input" id="customFile">
                <label class="custom-file-label" for="customFile">Choose file</label>
            </div>
            <span asp-validation-for=@Model.Image class="text-danger"></span>
        </div>

        <input type="submit" value="Send" />
    </form>


    <p>
        @Html.ActionLink("Delete", "Delete") |
        @Html.ActionLink("Back to List", "GetProducts")
    </p>
</div>

@section scripts{
<script type="text/javascript">

    // Add the following code if you want the name of the file appear on select
    $(".custom-file-input").on("change", function () {
        var fileName = $(this).val().split("\\").pop();
        $(this).siblings(".custom-file-label").addClass("selected").html(fileName);
    });
</script>
}