@model CategoriesByGenderViewModel


@*<nav class="navbar navbar-expand-sm navbar-light bg-white border-bottom mb-3">
    <div class="container justify-content-center">
        <ul class="d-inline-flex justify-content-center text-center">
            <li class="nav-item">
                <div class="dropdown">
                    <button class="dropbtn">Women's</button>

                    <div class="dropdown-content">
                        @foreach (var category in PModel.Women)
                        {
                            <a asp-controller="Home" asp-action="GetProductByCategory" asp-route-category=@category> @category.Name</a>
                        }

                    </div>
                </div>
            </li>

            <li class="nav-item">
                <div class="dropdown">
                    <button class="dropbtn">Men's</button>
                    <div class="dropdown-content">
                        @foreach (var category in Model.Men)
                        {
                            <a asp-controller="Home" asp-action="GetProductByCategory" asp-route-category=@category> @category.Name</a>
                        }

                    </div>
                </div>
            </li>
        </ul>
    </div>
</nav>*@